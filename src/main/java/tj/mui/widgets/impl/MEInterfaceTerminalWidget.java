package tj.mui.widgets.impl;

import gregtech.api.util.Position;
import gregtech.api.util.Size;
import tj.mui.widgets.TJWidget;

public class MEInterfaceTerminalWidget extends TJWidget<MEInterfaceTerminalWidget> {

    public MEInterfaceTerminalWidget(int x, int y, int width, int height) {
        super(new Position(x, y), new Size(width, height));
    }
}
